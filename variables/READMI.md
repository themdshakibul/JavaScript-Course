# JavaScript Variables (var, let, const)

JavaScript-এ ভেরিয়েবল ব্যবহার করে আমরা ডাটা স্টোর করি। JavaScript-এ মূলত **৩ ধরনের ভেরিয়েবল** আছে:

1. `var`
2. `let`
3. `const`

---

## 1️⃣ var

`var` হলো JavaScript-এর পুরোনো ভেরিয়েবল ডিক্লেয়ার করার পদ্ধতি।

### বৈশিষ্ট্য:

- Function scoped
- Re-declare করা যায়
- Re-assign করা যায়
- Hoisting হয় (undefined হিসেবে)

### Example:

```js
var name = "Md";
var name = "Shakibul"; // allowed
name = "Islam"; // allowed
```

### সমস্যা:

`var` block scope মানে না, তাই বড় প্রজেক্টে bug হওয়ার সম্ভাবনা বেশি।

---

## 2️⃣ let

`let` ES6 (2015) থেকে এসেছে এবং এটি বেশি নিরাপদ।

### বৈশিষ্ট্য:

- Block scoped `{ }`
- Re-declare করা যায় না
- Re-assign করা যায়
- Hoisting হয় কিন্তু ব্যবহার করা যায় না

### Example:

```js
let age = 25;
age = 26; // allowed

// let age = 30; ❌ error
```

### Block Scope Example:

```js
if (true) {
  let x = 10;
}
// console.log(x); ❌ error
```

---

## 3️⃣ const

`const` ব্যবহার করা হয় constant (পরিবর্তন হবে না এমন) ভেরিয়েবলের জন্য।

### বৈশিষ্ট্য:

- Block scoped
- Re-declare করা যায় না
- Re-assign করা যায় না
- Value অবশ্যই সাথে সাথে দিতে হয়

### Example:

```js
const country = "Bangladesh";

// country = "India"; ❌ error
```

### Object / Array ক্ষেত্রে:

```js
const user = { name: "Md Shakibul Islam" };
user.name = "shakibul"; // ✅ allowed
```

কারণ এখানে reference একই থাকে।

---

## 📊 var vs let vs const (Comparison)

| Feature    | var      | let   | const |
| ---------- | -------- | ----- | ----- |
| Scope      | Function | Block | Block |
| Re-declare | ✅       | ❌    | ❌    |
| Re-assign  | ✅       | ✅    | ❌    |
| Hoisting   | ✅       | ⚠️    | ⚠️    |

---

## ✅ Best Practice

- Default হিসেবে `const` ব্যবহার করুন
- যখন value পরিবর্তন হবে তখন `let`
- `var` এড়িয়ে চলুন

---

### ✨ Conclusion

Modern JavaScript-এ `let` এবং `const` সবচেয়ে বেশি ব্যবহার করা হয় কারণ এগুলো নিরাপদ এবং clean code লেখায় সাহায্য করে।

---

🧑‍💻 Happy Coding 🚀
