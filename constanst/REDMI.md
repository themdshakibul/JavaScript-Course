# JavaScript `const` (Constant) – Notes

`const` হলো JavaScript-এ ভেরিয়েবল ডিক্লেয়ার করার সবচেয়ে নিরাপদ উপায়। এটি ES6 (2015) থেকে এসেছে এবং modern JavaScript-এ সবচেয়ে বেশি ব্যবহার করা হয়।

---

## 🔹 const কী?

`const` দিয়ে এমন ভেরিয়েবল ডিক্লেয়ার করা হয় যার **reference পরিবর্তন করা যাবে না**।

```js
const name = "Shakibul";
```

---

## 🔹 const-এর প্রধান বৈশিষ্ট্য

* ✅ Block Scoped `{ }`
* ❌ Re-declare করা যায় না
* ❌ Re-assign করা যায় না
* ✅ Hoisting হয় (কিন্তু ব্যবহার করা যায় না)
* ✅ Value অবশ্যই সাথে সাথে দিতে হয়

---

## 🔹 Re-assign করা যায় না

```js
const age = 25;
age = 26; // ❌ Error
```

কারণ `const` ভেরিয়েবলের reference পরিবর্তন করা নিষিদ্ধ।

---

## 🔹 Re-declare করা যায় না

```js
const city = "Dhaka";
const city = "Chittagong"; // ❌ Error
```

---

## 🔹 Block Scope Example

```js
if (true) {
  const x = 10;
}

// console.log(x); ❌ Error
```

`const` শুধু তার নিজের block-এর ভেতরেই কাজ করে।

---

## 🔹 Object এবং Array এর ক্ষেত্রে `const`

👉 **গুরুত্বপূর্ণ বিষয়:** `const` মানে value পুরোপুরি change করা যাবে না — এটা ভুল ধারণা।

### ✅ Object Example

```js
const user = {
  name: "Shakibul",
  age: 24
};

user.name = "themdshakibul"; // ✅ allowed
```

### ✅ Array Example

```js
const numbers = [1, 2, 3];
numbers.push(4); // ✅ allowed
```

🔹 কারণ এখানে reference একই থাকে, শুধু ভিতরের value পরিবর্তন হয়।

---

## ❌ Object / Array পুরোপুরি change করা যাবে না

```js
const user = { name: "Shakibul" };
user = { name: "Mia" }; // ❌ Error
```

---

## 📌 কখন `const` ব্যবহার করবো?

* যখন ভেরিয়েবলের reference পরিবর্তন হবে না
* Default হিসেবে সবসময় `const` ব্যবহার করুন
* Clean এবং bug-free code লেখার জন্য

---

## 🆚 const vs let (Short Compare)

| Feature    | const           | let              |
| ---------- | --------------- | ---------------- |
| Scope      | Block           | Block            |
| Re-assign  | ❌               | ✅                |
| Re-declare | ❌               | ❌                |
| Use Case   | Fixed reference | Changeable value |

---

## ✅ Best Practice

```js
const API_URL = "https://api.example.com";
const users = [];
```

🔹 Constant নাম সাধারণত **UPPERCASE** লেখা হয়।

---

### ✨ Conclusion

Modern JavaScript-এ `const` সবচেয়ে বেশি ব্যবহার করা হয় কারণ এটি নিরাপদ, readable এবং maintainable code লেখায় সাহায্য করে।

---

🚀 Happy Coding
